<h1>Admin::Awardseason#index</h1>

<ul>
  <% @award_seasons.each do |season| %>
    <li>
      <%= link_to(season.name, admin_edit_award_season_path(season)) %>
    </li>
  <% end %>
  <hr />
  <li>
    <%= link_to 'New Award Season', new_admin_award_seasons_path %>
  </li>
</ul>
