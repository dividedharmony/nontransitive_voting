<h1>Admin::AwardCategory#index</h1>

<ul>
  <% @award_categories.each do |category| %>
    <li>
      <%= link_to(category.title, admin_edit_award_category_path(category)) %>
    </li>
  <% end %>
  <hr />
  <li>
    <%= link_to 'New Award Category', new_admin_award_categories_path %>
  </li>
</ul>
